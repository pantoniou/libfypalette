add_executable(fypalette-show fypalette-show.c)
target_link_libraries(fypalette-show PRIVATE fypalette)
if(ASAN_C_FLAGS)
    target_compile_options(fypalette-show PRIVATE ${ASAN_C_FLAGS})
    target_link_options(fypalette-show PRIVATE ${ASAN_C_FLAGS})
endif()
install(TARGETS fypalette-show RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})

add_executable(fypalette-probe fypalette-probe.c)
target_link_libraries(fypalette-probe PRIVATE fypalette)
if(ASAN_C_FLAGS)
    target_compile_options(fypalette-probe PRIVATE ${ASAN_C_FLAGS})
    target_link_options(fypalette-probe PRIVATE ${ASAN_C_FLAGS})
endif()
install(TARGETS fypalette-probe RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
